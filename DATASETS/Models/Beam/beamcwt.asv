%% Load data
beam = readmatrix('beam-1_3-1.csv')
fs = 2*494.71999999999997 ; % sampling frequency
% Add noise if desired
noise_amp = 0.5 ; 
dm = beam + noise_amp*randn(1, length(beam)) ;


% Wavelet computation
%cwt(beam, 'bump', fs) ;
[wt, fresp] = cwt(beam, 'bump', fs) ;
wt_amp = abs(wt) ;
csvwrite('cwt-beam-1_3-1.csv',wt_amp);
csvwrite('freq-beam-1_3-.csv',fresp)
